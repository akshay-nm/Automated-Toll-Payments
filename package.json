{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn lerna run start",
    "test": "yarn lerna run test",
    "build": "yarn lerna run build",
    "deploy": "yarn lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "automated-toll-payments"
}
